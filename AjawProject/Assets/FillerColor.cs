using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FillerColor : MonoBehaviour
{
    void Update()
    {
        /*if(this.gameObject.GetComponent<CharacterScript>().type == CharacterScript.HUMAN_TYPE.REPRODUCT)
            this.gameObject.transform.Find("barra energia/fill").GetComponent<Image>().color = Color.blue;
        else if (this.gameObject.GetComponent<CharacterScript>().type == CharacterScript.HUMAN_TYPE.FOOD)
            this.gameObject.transform.Find("barra energia/fill").GetComponent<Image>().color = Color.yellow;*/
    }
}
